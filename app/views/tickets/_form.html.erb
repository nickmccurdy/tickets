<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-horizontal">

    <div class="control-group">
      <label class="control-label" for="ticket-owner">Owner</label>
      <div class="controls">
        <input id="ticket-owner" name="ticket[owner]" type="text" size="30" placeholder="Owner">
      </div>
    </div>

    <div class="control-group">
      <label class="control-label" for="ticket-reason">Reason</label>
      <div class="controls">
        <input id="ticket-reason" name="ticket[reason]" type="text" size="30" placeholder="Reason">
      </div>
    </div>

    <div class="control-group">
      <div class="controls">
        <button name="commit" type="submit" class="btn btn-primary">Create ticket</button>
      </div>
    </div>

  </div>

<% end %>
