%tr
  %td= ticket.name
  %td= ticket.computer
  %td= ticket.reason
  %td

    %span.pull-left
      = distance_of_time_in_words_to_now ticket.created_at
      ago

    = button_to ticket, class: 'btn btn-danger pull-right', method: :delete do
      %i.glyphicon.glyphicon-remove
